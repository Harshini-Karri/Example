 step1: npm init command executed to generate package.json file with the necessary project metadata
 step2:touch index.js command executed to create the main JavaScript file
 step3: npm install command executed to install project dependencies it generates package-lock.json
npm i express 
expanation:  this command specifically installs the Express framework, a popular web application framework for Node.js, and adds it to the project's dependencies in the package.json file.As a result, it allows developers to build web applications more efficiently and effectively.